import { EnterLeaveDirective } from './enter-leave.directive';

describe('EnterLeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterLeaveDirective();
    expect(directive).toBeTruthy();
  });
});
